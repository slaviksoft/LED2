<resources>

    <color name="theme_background_color">#FFFFFFFF</color>
    <color name="theme_color">#FF00A84F</color>
    <color name="theme_color_half">#8000A84F</color>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/theme_color</item>
        <item name="colorPrimary50">@color/theme_color_half</item>
        <item name="android:windowBackground">@color/theme_background_color</item>

    </style>

    <attr format="color" name="colorPrimary50"/>

</resources>
